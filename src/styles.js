import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    flexGrow: 1,
    marginTop: '100px'
  },
  paper: {
    textAlign: "center",
    border:'none'
  },
  gridItem: {
    textAlign: 'center'
  }
}));
